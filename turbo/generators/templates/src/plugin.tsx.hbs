import {definePlugin, type Plugin} from 'sanity'

interface PluginConfig {
  name?: string
}

export const {{ pluginNamedExport }}: Plugin<PluginConfig> = definePlugin<PluginConfig>(
  (config = {}) => {
    const {name = '{{ name }}'} = config

    return {
      name,
      // ...
    }
  },
)
