---
'sanity-plugin-transifex': minor
---

Add a `defaultI18nArrayConfig` export (and `i18nArrayPatch` re-export) for documents using `sanity-plugin-internationalized-array` fields, and support both the legacy and new internationalized array / `translation.metadata` data formats via updated `sanity-translations-tab` and `sanity-naive-html-serializer` dependencies.
