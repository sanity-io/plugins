---
'sanity-plugin-transifex': minor
---

Support both the legacy and new internationalized array / `translation.metadata` data formats via updated `sanity-translations-tab` and `sanity-naive-html-serializer` dependencies.
