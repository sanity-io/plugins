---
'sanity-plugin-internationalized-array': patch
---

Pass `undefined` as the 4th `parentValue` argument to `filterField` to match updated `FilterFieldFunction` signature in `@sanity/language-filter`
