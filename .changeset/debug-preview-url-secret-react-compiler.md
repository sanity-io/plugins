---
"@sanity/debug-preview-url-secret-plugin": patch
---

Enable React Compiler in the build for consistency with the other plugins in the monorepo. No functional change: the plugin has no React components, so the compiled output is identical.
