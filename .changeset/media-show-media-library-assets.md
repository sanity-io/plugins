---
"sanity-plugin-media": minor
---

Add `showMediaLibraryAssets` option (default `true`). When set to `false`, assets managed by the Sanity Media Library (`source.name == "sanity-media-library"`) are excluded from the Media browser grid, asset picker queries, folder counts and realtime listeners.
