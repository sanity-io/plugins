---
"sanity-plugin-media": patch
---

Remove unused internal code and align the package exports with the monorepo convention (`publishConfig.exports` plus `development`/`source` conditions). No changes to the public API.
