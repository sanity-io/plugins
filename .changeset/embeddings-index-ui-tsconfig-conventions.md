---
"@sanity/embeddings-index-ui": patch
---

Adopt the shared monorepo TypeScript and lint conventions (shared `@repo/tsconfig`, type-aware linting) and fix the type errors surfaced by the stricter config.
