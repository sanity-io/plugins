---
"sanity-plugin-mux-input": patch
---

Replace deprecated Sanity UI, React, and RxJS APIs (`space` → `gap`, `useRelativeTime` with `useTemporalPhrase`, `throwError` factory). Remaining theme v1 focus-ring usage is deferred with targeted lint disables.
