---
"sanity-plugin-internationalized-array": patch
---

Remove unused dependency `fast-deep-equal`. Clean up internal cache module by removing dead code (exported functions that were never imported).
