---
"sanity-plugin-internationalized-array": minor
---

Add `fieldMenu` (`field_menu` alias) — a compact per-field add-translation control. One translate icon opens a language menu on stable `@sanity/ui`, without `document.unstable_fieldActions`.
