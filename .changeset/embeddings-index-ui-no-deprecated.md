---
"@sanity/embeddings-index-ui": patch
---

Replace deprecated Sanity UI and React APIs (`space` → `gap`, `FormEvent`). Keep `useProjectHostname: false` for the `embeddings-index-host` override, which has no non-deprecated replacement.
